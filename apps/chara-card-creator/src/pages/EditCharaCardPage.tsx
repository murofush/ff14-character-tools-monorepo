import { PageCard } from '../components/PageCard'

export function EditCharaCardPage() {
  return (
    <PageCard
      title="Edit Chara Card"
      description="旧 `/editCharaCard` 画面に相当する React SPA のルートです。"
    >
      <p>今後この画面にキャラカード編集 UI を段階的に移植できます。</p>
    </PageCard>
  )
}
