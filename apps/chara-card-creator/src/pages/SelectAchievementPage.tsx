import { PageCard } from '../components/PageCard'

export function SelectAchievementPage() {
  return (
    <PageCard
      title="Select Achievement"
      description="旧 `/selectAchievement` 画面に相当する React SPA のルートです。"
    >
      <p>達成項目セレクターの React コンポーネント移植先として利用します。</p>
    </PageCard>
  )
}
